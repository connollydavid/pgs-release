# .host-remap — adoption rename dictionary.
# The migration's substitutions were applied and retired; the durable copy is
# boxed in call/0001. This file stays comment-only so the remap recheck
# (host-lifecycle remap --check) audits the live tree against an empty map.
